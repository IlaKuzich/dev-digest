import type { CSSProperties } from "react";

/** Co-located styles for DashboardHome. */
export const s = {
  page: { padding: "24px 32px 44px", maxWidth: 1100, margin: "0 auto" } satisfies CSSProperties,
  header: { display: "flex", alignItems: "center", gap: 14, marginBottom: 24 } satisfies CSSProperties,
  headerText: { flex: 1 } satisfies CSSProperties,
  h1: { fontSize: 24, fontWeight: 700, letterSpacing: "-0.02em" } satisfies CSSProperties,
  subtitle: { fontSize: 14, color: "var(--text-secondary)", marginTop: 4 } satisfies CSSProperties,
  list: { display: "flex", flexDirection: "column", gap: 8, marginBottom: 32 } satisfies CSSProperties,
  columnHeader: {
    display: "flex",
    alignItems: "center",
    gap: 20,
    padding: "0 16px",
    marginBottom: 2,
  } satisfies CSSProperties,
  columnHeaderLabel: {
    width: 46,
    textAlign: "right",
    fontSize: 11,
    fontWeight: 700,
    letterSpacing: "0.06em",
    textTransform: "uppercase",
    color: "var(--text-muted)",
  } satisfies CSSProperties,
  row: {
    display: "flex",
    alignItems: "center",
    gap: 20,
    padding: "14px 16px",
    borderRadius: 9,
    border: "1px solid var(--border)",
    background: "var(--bg-elevated)",
    cursor: "pointer",
  } satisfies CSSProperties,
  iconBox: {
    width: 34,
    height: 34,
    borderRadius: 8,
    display: "grid",
    placeItems: "center",
    background: "var(--accent-bg)",
    color: "var(--accent)",
    flexShrink: 0,
  } satisfies CSSProperties,
  rowText: { flex: 1, minWidth: 0 } satisfies CSSProperties,
  rowNameLine: { display: "flex", alignItems: "center", gap: 8 } satisfies CSSProperties,
  rowName: { fontSize: 14.5, fontWeight: 600, color: "var(--text-primary)" } satisfies CSSProperties,
  rowSubtitle: {
    fontSize: 12.5,
    color: "var(--text-muted)",
    marginTop: 3,
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap",
  } satisfies CSSProperties,
  metricValue: { width: 46, textAlign: "right", fontSize: 14, fontWeight: 700 } satisfies CSSProperties,
  section: { marginTop: 8 } satisfies CSSProperties,
} as const;
