/** The exact heading `reviewer-core` renders the injected documents under
    (`reviewer-core/src/prompt.ts`) — named verbatim in the untrusted-block
    notice (AC-28) so the user sees the real prompt structure, not a paraphrase. */
export const PROJECT_CONTEXT_HEADING = "## Project context";
